import { future } from 'mdx-deck/themes'

const theme = {
  ...future,
  colors: {
    text: '#000',
    background: '#fff',
    link: '#fff',
    pre: '#fff',
    preBackground: '#051626',
    code: '#fff'
  }
}

export default theme